class ParserEnum:
    trace_port_flag = '--trace-port'
    trace_port_key = 'trace_port'

    ttl_max_flag = '--ttl-max'
    ttl_max_key = 'ttl_max'

    timeout_flag = '-t'
    timeout_key = 'timeout'

    target_flag = '--target'
    target_key = 'target'

    help_flag = '--help'
    help_key = 'help'
    help_key_text = 'help_text'

    error = 'error'
    error_message = 'error_message'
